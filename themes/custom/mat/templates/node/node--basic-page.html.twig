
{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
  ]
%}
{{ attach_library('classy/node') }}
{% if parent_title is not empty and parent_image is not empty %}
<section class="parent-header" style="background-image: url('{{ parent_image }}');">
  <div class="container">
    <p class="parent-title">{{ parent_title }}</p>
    <h1>{{ label }}</h1>
    {% if node.field_main_description %}
    <p class="main-description">{{ node.field_main_description.value }}</p>
    {% endif %}
  </div>
</section>
{% else %}
<section class="parent-header" style="background-image: url('{{ file_url(node.field_parent_background_image.entity.uri.value) }}');">
  <div class="container">    
    <h1 class="parent-title">{{ label }}</h1>
    {% if node.field_main_description %}
    <p class="main-description">{{ node.field_main_description.value }}</p>
    {% endif %}
  </div>
</section>
{% endif %}
<article{{ attributes.addClass(classes) }}>

  {% if display_submitted %}
    <footer class="node__meta">
      {{ author_picture }}
      <div{{ author_attributes.addClass('node__submitted') }}>
        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
        {{ metadata }}
      </div>
    </footer>
  {% endif %}

  <div{{ content_attributes.addClass('node__content') }}>

    {{ content|without('field_cover_image') }}
  </div>

</article>
